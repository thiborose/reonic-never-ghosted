# Strategy Contract

**Deferred until the frontend flow is defined.** The frontend journey defines
what the backend must return and the API/SLA between them — so this gets written
*after* the flow, not before.

What's stable to carry in: the coach output is a **communication strategy** =
ordered steps, each with a reason and supporting evidence (see
[`../metadata.md`](../metadata.md)). Exact fields + endpoints: TBD.

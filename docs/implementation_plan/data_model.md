# Data Model

**Deferred.** The entity/Pydantic shape depends on the frontend flow and the
agent-vs-LLM decision — defining it now just gets rewritten.

Source of truth for *what data and why*: [`../metadata.md`](../metadata.md).
Fill this in once the frontend flow pins down what the backend must serve.
